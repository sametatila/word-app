# Durum takibi

Her ajan aldığı WP'yi buraya işler: durum (`bekliyor` → `sürüyor` → `inceleme` → `bitti`), sahip, başlangıç, son commit, not. Bitmiş sayılma ölçütü README'deki DoD.

| WP | Başlık | Faz | Durum | Sahip | Başlangıç | Son commit | Not |
|---|---|---|---|---|---|---|---|
| WP-00 | Öğrenme ölçüm çerçevesi | 0 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `kind` sütunu eklendi; `session_round` yazılmıyor (karar kaydı) |
| WP-01 | Beceri ilerlemesi sunucuya | 0 | inceleme | Claude | 2026-08-25 | (bkz. git log) | GET/POST/PUT `/api/skills`, `lib/skills/record.ts`, e2e §27 |
| WP-02 | Hata taksonomisi | 0 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/errors.ts`, `reviews.error_type/detail`, 11 oyun + yürüyüş, e2e §28 |
| WP-03 | AI değerlendirme servisi | 0 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `/api/assess`, `assessments` tablosu, istemci yedeği; kalite testi 20/20 (assess-samples.md) |
| WP-04 | Çevrimdışı rol yapma yedeği | 0 | inceleme | Claude | 2026-08-25 | (bkz. git log) | senaryo motoru + 10 A1 senaryosu; kanıt `reports/shots/wp04-roleplay-*.png` |
| WP-10 | Çeviri oyunu | 1 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `translate` turu, `lib/sentence-match`, kısmi SRS kalitesi, AI onayı; kanıt `reports/shots/wp10-translate-*.png` |
| WP-11 | Dönüştürme drilleri | 1 | inceleme | Claude | 2026-08-26 | (bkz. git log) | `lib/cheatsheet/drills.ts` 5 tablo × 12, `drill-player.tsx`, `/cheatsheet/[table]/drill`, `npm run test:cheat`, ilerleme `cheat_progress` (d:… kimlik), `drill` olayı; kelime turuna sızma ve ders özeti köprüsü ertelendi; kanıt `reports/shots/wp11-*.png` |
| WP-12 | Serbest cümle görevi | 1 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `free_sentence` turu, `AssessmentCard`, yazma görevi `kind:"sentence"`; kanıt `reports/shots/wp12-free-*.png` |
| WP-13 | "Neden" geri bildirimi | 1 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/why.ts`, `FeedbackLine`, şerit 2. satır, cheatsheet derin bağlantı; kanıt `reports/shots/wp13-*.png` |
| WP-14 | Oyun merdiveni | 1 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/ladder.ts`, yazarak tamamla, yeni kelimeye ipuçlu yazma, oturum içi basamak inişi; kanıt `reports/shots/wp14-*.png` · 26 Ağu: üretim tabanı `PRODUCTION_FLOOR` (%40) — zorlanan öğrenci simülasyonunda %28 → %45; KPI 2 tanımı tur sayar (tanıtım hariç, eşleştirme 1 tur) |
| WP-20 | Telaffuz puanlama | 2 | inceleme | Claude | 2026-08-26 | (bkz. git log) | Faz 1: `lib/stt.ts` zinciri (Groq → Cloudflare → Speechmatics → Deepgram → Mistral), `/api/pronounce` + `lib/pronounce.ts` (kelime/bütünlük/akıcılık, `confusions` ipucu), söyleyiş turunda paralel kayıt + ısı haritası, pocket-mic WAV; `test:pronounce`, `report:stt`; fonem düzeyi (HF Space) ve sınav bağlantısı faz 2; kanıt `reports/shots/wp20-pronounce-card.png` |
| WP-21 | Konuşma içeriği + monolog | 2 | kısmen | Claude | 2026-08-26 | (bkz. git log) | `SpeakingMonologueExercise` + doğrulayıcı, `monologue-player.tsx` (hazırlık 30 sn → kayıt → transkript → rubrik; tanıyıcısız öz değerlendirme yedeği), 20 monolog (seviye başına 4) üretime yüklendi; 40 drill (WP-72 içerik) ve telaffuz puanı (WP-20) bekliyor; kanıt `reports/shots/wp21-*.png` |
| WP-22 | Rol yapma sınav modu | 2 | inceleme | Claude | 2026-08-26 | (bkz. git log) | `mode: exam` istemi (yardım/düzeltme/Türkçe yok), `/lessons/[id]/exam` (5 tur, 3 dk, mikrofon veya yazı), toplu rubrik puanı `assessments`'a (kind roleplay), sonuç kartı (en iyi 2 cümle, en sık 2 hata, can-do); `roleplay_logs.mode` (0034); telaffuz ortalaması WP-20, seviye sınavı konuşma bölümü bağlantısı sonra; kanıt `reports/shots/wp22-exam-*.png` |
| WP-23 | Açık diyalog motoru | 2 | inceleme | Claude | 2026-08-26 | (bkz. git log) | `DialogueTheme` (7 diyalogda), `/api/roleplay` `exerciseId` + `dialoguePrompt`, oynatıcıda LLM/senaryo anahtarı (aynı UI, servis koparsa senaryo), kalıp kullanımı yerel eşleştirme (`targetsUsed`), tamamlanma ≥4 tur ve ≥3 kalıp (üst sınır 8), yazarak cevap; 7 → 25 diyalog içeriği WP-72'de; kanıt `reports/shots/wp23-dialogue-*.png` |
| WP-30 | AI yazma değerlendirmesi | 3 | inceleme | Claude | 2026-08-25 | (bkz. git log) | serbest görev → rubrik kartı, kuyruk + cron, Yazılarım; kanıt `reports/shots/wp30-writings.png` |
| WP-31 | Yazma / soru türleri | 3 | inceleme | Claude | 2026-08-26 | (bkz. git log) | Tipler, oynatıcılar, doğrulayıcı; her yazma egzersizine yeni tür görev (`writing-extra.ts`), okuma/dinlemeye yazılı sorular (`derived-questions.ts`); kanıt `reports/shots/wp31-*.png` |
| WP-40 | Yerleştirme testi | 4 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `/placement`, `lib/placement{,-score}.ts`, `placements` tablosu, profil kartı; kalibrasyon (10 kişi) bekliyor; kanıt `reports/shots/wp40-placement-*.png` |
| WP-41 | Seviye ve modül sınavı v2 | 4 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/exam.ts`, `/exam/[level]{,/[module]}`, `exam-player`, sertifika SVG; konuşma bölümü eklendi (26 Ağu: 2/3 söyleyiş cümlesi, /api/pronounce, STT yoksa bölüm yok); rozet yok; kanıt `reports/shots/wp41-exam-*.png` |
| WP-42 | Haftalık kullanım sınavı | 4 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `/learn/haftalik`, `lib/weekly.ts`, `exams` tablosu, plan kartı öğesi; quest/bildirim yapılmadı; kanıt `reports/shots/wp42-weekly.png` |
| WP-43 | CEFR can-do haritası | 4 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/cando.ts` 121 ifade, kural tabanlı etiket haritası, `GET /api/cando`, profil kartı; kanıt `reports/shots/wp43-cando.png` |
| WP-50 | Beceri yetkinlik modeli | 5 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/proficiency{,-data}.ts`, `GET /api/profile`, profil çubukları, plan öneri motoru; kanıt `reports/shots/wp50-proficiency.png` |
| WP-51 | Hata analitiği | 5 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/error-analytics.ts`, `GET /api/errors`, profilde Zayıf noktaların, SRS sık-hata ağırlığı + `srs_weight` olayı; haftalık bildirim yapılmadı; kanıt `reports/shots/wp51-weak-spots.png` |
| WP-52 | Gelişim raporu | 5 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/growth.ts`, `GET /api/growth`, profil Gelişim kartı (SVG çizgiler), plan kartında Pzt–Sal özet, `/api/cron/summary` push; kanıt `reports/shots/wp52-growth.png` |
| WP-60 | /learn yeniden kompozisyon | 6 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `lib/plan.ts`, `GET /api/plan`, `PlanCard`; modlar şeridi/sınav kartı yapılmadı (karar kaydı); kanıt `reports/shots/wp60-plan.png` |
| WP-61 | Geri bildirim bileşeni | 6 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `feedback/{feedback-line,diff-text,assessment-card}.tsx`, `/demo-feedback`; kanıt `reports/shots/wp61-feedback.png` |
| WP-62 | Ders oynatıcı akışı | 6 | kısmen | Claude | 2026-08-25 | (bkz. git log) | Mikrofon 4 sn sessizlikte yazma alanı açılır (mikrofon açık kalır), adım türü renkli ilerleme + lejant, özette kullanılan kalıplar + can-do + "Sıradaki ders"; adım türü karışımı (WP-71 içerik), transform/choose (WP-11), drill köprüsü (WP-11), sınav modu (WP-22) bekliyor; kanıt `reports/shots/wp62-lesson-progress.png` |
| WP-63 | Beceri merkezi | 6 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `/skills`: yetkinlik panosu + önerilen sıradaki, 6 sekme (4 beceri + Dilbilgisi + Sınav), yapılmamış/geliştir/tamam süzgeci, kartta tür/can-do/son puan; drill sekmesi WP-11'i bekliyor; kanıt `reports/shots/wp63-hub-*.png` |
| WP-64 | Profil ve analitik | 6 | inceleme | Claude | 2026-08-25 | (bkz. git log) | Bölüm sırası plana göre (yetkinlik → zayıf → gelişim → can-do → sınavlarım → seviye testi → rozet → kelime → yazılarım → ayarlar), `exams-card.tsx` (sınav arşivi + sertifika + haftalık çubuklar); grafikler satır içi SVG (WP-52); kanıt `reports/shots/wp64-*.png` |
| WP-65 | Onboarding + yerleştirme akışı | 6 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `course-onboarding.tsx` 4 adım (isim/kurs/ses → hedef → ölç/seç → hazır), `profiles.goal` (0033), Erdi rehber; 3 kişilik test sahibinde; kanıt `reports/shots/wp65-onboarding-{1..4}.png` |
| WP-66 | Erdi koç sesi | 6 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `coach-bubble.tsx`, `lib/coach-lines.ts` (40 cümle, tekrar korunumu), 5 an bağlı; `/learn?game=` parametresi ilk kez okunuyor; kanıt `reports/shots/wp66-coach-*.png` |
| WP-70 | İçerik şeması ve doğrulayıcı | 7 | inceleme | Claude | 2026-08-25 | (bkz. git log) | `data/content/SPEC.md`, `npm run test:content`, etiket başına uyarı bütçesi; paket/apply genelleştirme WP-71 pilotunda |
| WP-71 | Ders kapsamı B1–C1 | 7 | sürüyor | Claude | 2026-08-26 | (bkz. git log) | **KAPANDI — A1'den C1'e 500 ders, 50 modül sınavı** (B1, B2 ve C1 26 Ağustos'ta üretildi). Ders başına 5 kelime tekrarı + 4 üretim + 1 doğru/yanlış; modül başına 3 doğru / 7 yanlış hüküm. Seviye odakları: B2 kurum dili (edilgenin bütün hâlleri, isimleşme, ortaç sıfatları, aktarım kipi, öznel kipler), C1 ise kural değil SEÇİM (işlev fiili öbekleri, kip parçacıkları, retorik, deyimler, tam dolaylı aktarım, hukuk dili, katılım öbekleri, imtiyaz, ironi). Kalan |
| WP-72 | Konuşma/yazma/okuma içeriği | 7 | sürüyor | Claude | 2026-08-26 | (bkz. git log) | Telaffuz drill 10 → 50 (+40), monolog 20, diyalog 7 → 25 (+18), yazma her egzersize yeni tür görev (40), okuma/dinlemeye 412 türetilmiş yazılı soru (`content:derive`); kalan: gerçek ses kayıtları, 51 egzersizde ikinci yazılı soru |
| WP-73 | Gerekçe ve kural parçacıkları | 7 | sürüyor | Claude | 2026-08-26 | (bkz. git log) | Kural şeması + 35 kural; drill 28/60 tablo (A1 15, A2 13 tamam; B1–C1 32 tablo kaldı); 150 karıştırma çifti + 1 458 aday (`content:confusables`) |
| WP-80 | Takip katmanı (veri boşluğu kapatma) | 8 | inceleme | Claude | 2026-08-26 | (bkz. git log) | 19 yeni olay (ekran, süre, ders adımı, söyleyiş kararı, ayar, cihaz, hata, bildirim hunisi), `lib/screens.ts`, `components/telemetry.tsx`, `test:events`, `report:events`/`report:learning` yeni bölümler, `report:all`; `docs/plan/80-takip.md` |

## Karar kaydı

Plan uygulanırken alınan ürün/teknik kararlar (tarih, karar, gerekçe, kim):

- 2026-08-25 — Plan yazıldı; kaynak `reports/ogrenme-kesif-raporu.html`.
- 2026-08-25 — `events` tablosuna `kind text` sütunu (kapalı sözlük etiketi, ≤32 karakter, sunucuda doğrulanır). Gerekçe: üretim türü+puan, beceri+seviye+puan gibi iki boyutlu olaylar tek tam sayıya sığmıyordu; serbest metin yasağı sürüyor. Claude.
- 2026-08-25 — `session_round` olayı yazılmıyor: `reviews` zaten oyun/doğruluk/gecikmeyi satır satır tutuyor, ikinci kopya yalnız hacim üretirdi. Ad listede duruyor, KPI'lar `reviews`'dan okuyor. Claude.
- 2026-08-25 — `reviews.detail` sütunu: yanlışın kendisi (seçilen şık / yazılan kelime, ≤ 60 karakter). Plan yalnız `error_type` diyordu; WP-51'in karıştırma çiftleri ("meaning" hatasında hangi kelime seçildi) bu olmadan hesaplanamazdı. Serbest metin değil: şık metni ya da tek kelime. Claude.
- 2026-08-25 — SRS hata ağırlığı: `schedule(prev, q, now, errorWeight)`; kelimenin son 14 gündeki son yanlışının tipine göre (`ERROR_SRS_WEIGHT`, artikel/çoğul 0,9, gerisi 1). Yalnız tekrar evresi gün aralığına uygulanır. WP-51 ölçüme göre ayarlar. Claude.
- 2026-08-25 — `/api/assess` JSON'u sağlayıcının JSON modundan değil istemden istiyor (`assess-prompts.ts`) ve toleranslı ayrıştırıcıyla okuyor; span'ler modelden değil, modelin verdiği "wrong" parçasının metinde aranmasıyla hesaplanıyor (karakter indeksi modellerde güvenilmez). Geçersiz çıktı 502. Claude.
- 2026-08-25 — Değerlendirme önbelleği bellek değil `assessments` tablosu (hash, 24 sa): sunucusuz ortamda süreç belleği paylaşılmıyor; tablo zaten gelişim grafiği için gerekiyordu. Kota önbellek isabetlerini saymıyor. Claude.
- 2026-08-25 — WP-03 kalite testi (20 örnek) yerelde koşulamadı: sohbet anahtarları yalnız Vercel'de (Sensitive). Betik ve insan puanları hazır (`npm run test:assess`, `docs/plan/assess-samples.md`); anahtarı olan koşup tabloyu doldurur. Claude.
- 2026-08-25 — Çevrimdışı rol yapmada anlaşılmayan cevap da tur sayılıyor (modelli akışla aynı kural: her kullanıcı sözü bir tur). Ders geçme kalıp kullanımına değil tur sayısına bağlı kaldı; kalıp kullanımı `production_attempt(roleplay)` puanı olarak ölçülüyor — WP-22 sınav modu bunu koşula çevirebilir. Claude.
- 2026-08-25 — Senaryolar ders dosyasına değil `content/scripts-a1.ts`'e yazıldı; `lessons/index.ts` kimlikle bağlıyor. İçerik hattı (WP-70/71) senaryoyu ders metninden bağımsız üretip gözden geçirebilsin diye. Claude.
- 2026-08-25 — Faz 0 e2e için yerel Postgres: `docker run … postgres:16-alpine` (5439), `DATABASE_URL=<test> npx drizzle-kit migrate`, `TEST_DATABASE_URL=<test> npm run test:seed && npm run test:e2e`. README'ye taşınmalı (WP-70 sırasında). Claude.
- 2026-08-25 — WP-03 kalite testi Mistral ile koşuldu: 20/20 ±1, hata tipi 14/14, span 11/11. Modelin JSON'u beş biçimde bozduğu ölçüldü; ayrıştırıcı tek kuralla (`closesString`) onarıyor, istem iç tırnağı yasaklıyor, bütçe 1600 jeton. Model insan puanından ~+1 cömert (WP-50 notu). Claude.
- 2026-08-25 — Sıra: Faz 0'dan sonra WP-70 yerine WP-13 alındı (sahibi "devam et" dedi; kullanıcıya en görünür kazanç geri bildirim). WP-70 sıradaki. Claude.
- 2026-08-25 — WP-13 adım 3 (okuma/dinleme sorusu gerekçesi): `SkillQuestion.explain` zaten cevaptan sonra Türkçe gerekçe gösteriyor; ayrı `why_tr` alanı açılmadı, WP-70 şemasında `explain` bu rolü üstlenir. Adım 4 (ders `produce`): ilk yanlışta içerikteki hedefe özgü ipucu zaten "neden" işlevi görüyor; dokunulmadı. Claude.
- 2026-08-25 — Artikel kuralları `lib/why.ts`'te "hep/genelde" güçlü/zayıf işaretli ve istisnada dürüst ("istisna — kelimeyle ezberle"); 50 isimlik test 35+ kural isabetiyle sabitlendi. Çoğul kalıbı turun doğru biçiminden türetiliyor (`formen` demo/dış veride eksik olabiliyor). Claude.
- 2026-08-25 — İçerik doğrulayıcı uyarıları toplam değil ETİKET başına bütçeliyor (`data/content/baseline.json`): toplam tavan, bir borcu azaltırken başka yerde borç almaya izin verirdi. Mevcut borç 2.227 uyarı (en yok 1.254, metinde geçmeyen sözlükçe 485, çok anlamlı tr 163, parantezli tr 158…); WP-72 kapatır. Claude.
- 2026-08-25 — Almanca metinde Türkçe harf kuralı özel adları (büyük harfle başlayan kelimeler) dışarıda bırakıyor: metinlerdeki "Frau Yıldız" bilinçli. Claude.
- 2026-08-25 — WP-70 adım 4 (paket/apply genelleştirme) bu turda yazılmadı: mevcut `data/skills/make-packets.mjs` + `apply-skills.ts` deseni SPEC'te akış olarak tarif edildi; ilk yeni içerik türü (WP-71 B1 pilotu) gelince genelleştirilecek — soyut bir üretici yazmak, tüketicisi olmadan yanlış soyutlama riski. Claude.
- 2026-08-25 — Çeviri turunda sıra hatası `correct=false` + `quality=3`: istatistikte ve hata tipinde yanlış sayılır (öğrenci cümleyi kuramadı), SRS'te kelime lapse etmez (kelime bilinmiş). `Answer.quality` sunucuda 0–5'e kilitlenir; yanlış cevap 3'ü aşamaz, doğru cevap 3'ün altına inemez. Claude.
- 2026-08-25 — Çeviri AI onayı: yerel hakem "yanlış" dediğinde ve cümle ≥ 3 kelimeyse `/api/assess` (sentence) 6 sn tavanla sorulur; overall ≥ 75 ve task ≥ 3 ise kabul (kalite 4, "anlamca doğru — başka kuruluş"). Sağlayıcı yok/zaman aşımı → yerel karar. Kota bu yolla tüketilir; `report:learning` üretim satırında görünür. Claude.
- 2026-08-25 — WP-14 A/B bayrağı (`profiles.flags.ladder`) yazılmadı: `profiles` tablosunda `flags` sütunu yok, salt bunun için şema açmak gereksiz; merdiven herkese açık, etkisi `report:learning` KPI 2 (üretim payı) ve doğrulukla izlenir. Claude.
- 2026-08-25 — KPI 2 tanımı genişledi: üretim = `production_attempt` olayları + `reviews` içinde üretim oyunları (`lib/ladder.ts PRODUCTION_GAMES`: typing/scramble/order/translate/speak). Önce yalnız olaylar sayılıyordu ve kelime turundaki üretim görünmez kalıyordu. Claude.
- 2026-08-25 — Yeni kelimenin ipuçlu yazma turu kuyruğun sonuna değil iki kelime sonrasına serpiştiriliyor: 10 yeni kelime × 3 tur = 30 > 20 tavan, sona konunca hiç çıkmıyordu (e2e ile ölçüldü). Claude.
- 2026-08-25 — Oturum içi basamak inişi istemcide: sunucu turları önceden kurduğu için `easeRound` eldeki veriyle dönüştürür (çeviri → cümle diz, yazarak tamamla → şıklı, yazma → ipuçlu); cevap oynanan oyunun adıyla kaydedilir. Claude.
- 2026-08-25 — `free_sentence` turu PLAYABLE dışında ve oturumda en çok 2: hakemi AI (kota + süre), sağlayıcı yokken tur hiç kurulmuyor (yedek dilbilgisini ölçemiyor; ölçülmeyen iş yaptırmak yerine tur yok). Beceri tarafındaki `kind:"sentence"` görevi ise sağlayıcı yokken yedekle çalışır ve "AI kapalı" der. Claude.
- 2026-08-25 — Serbest cümle SRS eşlemesi: overall ≥ 90 → 5, ≥ 70 → 4 (doğru), 40–69 → 3 (yanlış, lapse yok), < 40 → 2; yedekte en çok 3. Hata tipi rubriğin ilk hatasından. Claude.
- 2026-08-25 — WP-60 kapsam: plan kartı (adım 1–2, 5) yapıldı; modlar şeridi (adım 3) ve sınavlar kartı (adım 4) yapılmadı — mevcut "Başka türlü oyna" bölümü ve tek oyun seçici zaten görünür, sınavlar WP-40/41/42 gelmeden boş bir kart olurdu. `dailyStats.plan_done` sütunu açılmadı: "yapıldı" bugünün kayıtlarından türetiliyor (session_done olayı, ders/egzersiz lastAt, oyun cevapları); ayrı bir kayıt planı sözleşmeye çevirirdi. Öneri motoru WP-50 gelene kadar geçici kural: en az çalışılan beceri + en sık hata tipi (≥5/14 gün) → tek oyunlu tur. Claude.
- 2026-08-25 — Groq varsayılan modeli `llama-3.3-70b-versatile` → `openai/gpt-oss-120b`: eski model Groq'tan kaldırılmış (404), zincirdeki yedek sessizce ölüydü; WP-30 kalite testinde Mistral 429 verince görüldü. Sağlayıcı model kataloğu için `report:providers`'a "404 model_not_found" uyarısı eklenmeli (WP-52 ile). Claude.
- 2026-08-25 — Rubrikte `task` puanı dilbilgisinden ayrıldı ("içerik varsa hatalı da olsa 4"): gpt-oss hatalı metinlerde görevi de düşürüyordu. Claude.
- 2026-08-25 — Yazma kuyruğu: `assessments.result` null = bekleyen; `/api/assess/queue` yazar, `/api/cron/assess` (6 saatte bir, `vercel.json`) puanlar ve push bildirir. Kuyrukta görev metni saklanmıyor (yalnız tür/seviye/metin); gecikmeli değerlendirme görev puanında yaklaşık — kart "gecikmeli" der. Claude.
- 2026-08-25 — Can-do etiketleme ilk turda KURALLA (`lib/cando-map.ts`: dersin simgesi+seviye+focusId, egzersizin beceri+seviye+tür); içeriğin kendi `cando` alanı varsa o kazanır. 564 içeriğe LLM+insan etiketi WP-71/72 gözden geçirmesine bırakıldı; harita o zaman küçülür. Kanıt eşiği: ≥2 bağlı içerik tamamlandı → kanıtlı, 1 → gelişiyor; sınav bölümü (WP-41/42) gelince üçüncü kanıt türü. Claude.
- 2026-08-25 — Yerleştirme testi yazma/konuşma aşamaları (isteğe bağlı) bu turda yok: yazma WP-03 ile bağlanabilir ama süreyi 15 dk sınırına yaklaştırıyor, konuşma WP-20'ye bağlı. Kelime aşaması "biliyorum/emin değilim" öz bildirimi yerine doğrudan 6 kontrol sorusu: öz bildirim + kontrol iki kat süre alıyordu, ölçtüğü şey aynı. Dilbilgisi maddeleri WP-11 drill havuzu yerine dilbilgisi tablosu hücrelerinden (`CHEAT_ITEMS`, ≥3 kardeşli). Kalibrasyon (10 kişi) proje sahibinin çevresiyle; eşikler `placement-score.ts` (`PASS_RATIO` 0,75, alt medyan). Claude.
- 2026-08-25 — Yerleştirme maddelerinde doğru cevap istemciye gider: test bir kendini ölçme, sınav değil; sunucu yine puanlar ve olay atar. Claude.
- 2026-08-25 — Haftalık sınav yanlışları SRS'e kalite 2 ile döner (lapse: aralık 0, pekişmişten düşer) — plan böyle; "dürüst ölçüm". Sesli Söyle sorusu WP-20 gelene kadar yazmaya düşer; serbest cümle soruları yalnız sağlayıcı varken (yoksa yazma). Quest kartı ve push bildirimi bu turda yok: plan kartı öğesi (WP-60) girişi sağlıyor; bildirim WP-52 haftalık özetle birleşir. Claude.
- 2026-08-25 — Yetkinlik modeli 6 beceri (okuma/dinleme/yazma/konuşma/dilbilgisi/kelime): plan 5 diyordu, haftalık kullanım sınavı kelime kanıtı olduğu için "kelime" ayrıldı. Kaynak eşlemesi: `user_skills.last_score` egzersiz ×1; `assessments` writing→yazma, sentence→dilbilgisi, speaking/roleplay→konuşma ×2; `exams` weekly→kelime ×3. Yerleştirme kanıta girmiyor (beceri başına puan değil seviye tahmini). Kanıtı olmayan hücre null ("ölçülmedi"), sıfır değil. Claude.
- 2026-08-25 — SRS hata ağırlığı artık ölçüme bağlı: kelimenin son yanlış tipi son 14 günde ≥5 kez görüldüyse aralık ×0,75 (`FREQUENT_ERROR_*`), yoksa tip varsayılanı (artikel/çoğul 0,9). Uygulanan her ağırlık `srs_weight` olayı (kind=tip, value=ağırlık×100) — `report:learning` KPI 7 altında listeleniyor; bayrak yok, olayla izlenir. Claude.
- 2026-08-25 — Hedefli çalışma "10 soruluk drill" yerine o hata tipinin tek oyunlu turu (20 tur) — WP-11 drill motoru gelince dilbilgisi tipleri drill'e yönlenir (`ERROR_TARGET_GAME` tek harita). Haftalık "en çok X hatası yaptın" bildirimi WP-52 özetine bırakıldı. Claude.
- 2026-08-25 — Gelişim grafikleri satır içi SVG (kütüphane yok, `dataviz` becerisi çağrılmadı): dört küçük çizgi ve boş hafta = boşluk (sıfır değil). Haftalık özet kartı `/learn` plan kartının altında yalnız Pzt–Sal; push `/api/cron/summary` Pazartesi 07:00 (UTC), geçen hafta aktif olanlara. "Yazılarım" WP-30'da yapılmıştı. Claude.
- 2026-08-25 — Sınav v2 konuşma bölümü yok (WP-20/22 bekliyor); dilbilgisi bölümü WP-11 drill'leri yerine dilbilgisi tablosu hücreleri (şıklı); yazma bölümü yalnız sağlayıcı varken kâğıda girer. Kayıt `exams` tablosuna kind `module:A1:0` / `level:A1`, week = sınav günü (aynı gün tekrar satırı günceller); bölümler/geçme/deneme `answers` jsonb'de — ayrı sütun açmadan; rozet grubu genişletilmedi. Sertifika SVG (`/api/certificate/[id]`), PDF yok: tarayıcı "yazdır" aynı sonucu verir. Kelime bölümünde "İpucu" düğmesi hâlâ görünür (GameShell ortak); sınavda ipucu kaliteyi düşürüyor ama bölüm puanını değil — WP-62/63 kompozisyonunda kapatılacak. Claude.
- 2026-08-25 — Onboarding 4 adım, her adımda tek karar: ses seçimi ayrı ekran değil, 1. adımda kursun altında (kurs değişince ses listesi değişiyor, ikisi bir karar). "Seviyemi ölçelim" profili A1 ile kaydedip `/placement`'a gider; test bitince seviye orada kabul edilir, onboarding'e dönülmez (ekran sayısı sabit kalsın). Hedef (`profiles.goal`: work/daily/exam/swiss) şimdilik yalnız kaydediliyor ve son ekranda yansıtılıyor; görev/içerik önerisini etkilemesi WP-60 plan kartı + WP-71/72 içerik etiketleriyle. Claude.
- 2026-08-25 — Koç balonu 4 sn sonra kapanır ama Erdi kalır (idle'a geçer); hareket azaltmada Erdi klibi yok, yalnız kalıcı metin. Balon sahne almaz (yerleşik Erdi): yürüyüş/kutlama sahneyi alınca Erdi kaybolur, cümle durur. Bir kartta tek Erdi: plan kartında Pzt–Sal özet varsa selam yerine `peek` + "bak ne oldu"; hedefli tur özetinde başlıktaki Erdi balonlu Erdi'ye dönüşür (ikinci Erdi eklenmez). Cümleler `{name}/{pct}/{level}` yer tutuculu, seçim cihazda son (n-1) cümleyi dışlar (`wortspiel-coach-seen`). Yan bulgu: `/learn?game=…` (WP-51 zayıf nokta, WP-60 plan öğesi) hiç okunmuyordu, karışık tura düşüyordu — `readGameParam` ile düzeltildi; hedefli turun ölçütü bu parametre. Claude.
- 2026-08-25 — Beceri merkezi: varsayılan sekme çalışma seviyesinde en zayıf egzersiz becerisi (ölçülmemiş > en düşük puan; `weakestSkill`), kullanıcı seçimi oturum boyunca kazanır. "Tamam" eşiği son puan ≥ %70 (yetkinlik "sağlam" bandıyla aynı çizgi), son puan yoksa en iyi doğru oranı. Egzersiz türü etiketi beceriden türetiliyor (meta'da soru türü yok; WP-70 `kind` alanı içerik geldikçe dolacak, o zaman etiket madde türünden). Can-do kartta tek satır: haritanın ilk ifadesi. Dilbilgisi sekmesi drill yerine dilbilgisi tabloları girişi + yetkinlik hücresi (WP-11 bekliyor). Sınav sekmesi seçili seviyenin sınavı + haftalık + yerleştirme + son 5 sınav ve sertifika. "Telaffuzda zorlandıkların" yalnız Konuşma sekmesinde. Claude.
- 2026-08-25 — Profil sırası: rozetler ve kelime grafikleri en üstten ortaya indi; öğrenme ölçümleri (yetkinlik, zayıf nokta, gelişim, can-do) önde. "Sınavlarım" seviye/modül sınavlarını ve haftalık kullanım sınavını tek kartta tutar (son 8'er); seviye testi kartı ayrı kaldı (ölçüm, sınav değil). Grafik için `dataviz` becerisi çağrılmadı: WP-52'nin satır içi SVG'si yeterli, kütüphane eklenmedi. Yazı arşivi silme WP-30'dan geliyor (`DELETE /api/assessments`). Claude.
- 2026-08-25 — Ders oynatıcı: yazma alanı 4 sn sessizlikte kendiliğinden açılıyor ama mikrofon kapanmıyor (12 sn sessizlik sınırı yerinde) — iki çıkış yolu aynı anda açık, öğrenci seçiyor. İlerleme çubuğu adım başına parça, renk adım türü; `de-a1-mein-tag`'da tekrar 8 / üret 3 / doğru-yanlış 1 — planın "%73 tekrar" tespiti görünür oldu, karışımı değiştirmek WP-71'in işi (oynatıcı türü çizer, seçmez). Özette "pekiştir: 5 soruluk drill" WP-11 motoru gelene kadar YOK; yerine kullanılan/kullanılmayan kalıplar (konuşma turlarında kalıp gövdesi araması) ve can-do ifadesi; "Sıradaki ders" `nextLesson` ile sunucudan. Rol yapma sınav modu düğmesi WP-22 ile. Claude.
- 2026-08-26 — Drill maddeleri `cheat_progress`'e kelime hücreleriyle aynı SRS motoruyla yazılıyor (kimlik `d:tablo:nn`, ayrı tablo yok); doğru = `matchSentence` exact/spelling (yazım sapması kuralı bozmaz), order/wrong yanlış. Boşluk türünde yalnız boşluğun kendisi de kabul (alternatif). Doğrulayıcı cevabın YA DA gerekçenin tablodaki bir biçime dokunmasını ister — "ist gegangen" tabloda yok ama gerekçesi "sein" der. Kelime turuna `transform` sızması (WP-14 merdiveni) ve ders özetinden drill köprüsü (WP-62) ertelendi: ders `focusId` ↔ tablo eşlemesi yok, önce WP-73 kural şemasında kurulacak. Claude.
- 2026-08-26 — Kural parçacıkları bağlama göre seçiliyor (`ruleFor(tip, cümle)`: pattern'li kurallar sırayla, ilk uyan; yoksa tipin genel kuralı). Sıra anlamlı — "bin gegangen" Perfekt yardımcı fiil kuralı, sein çekimi kuralından önce gelir. `why.ts` artikel/çoğul/yazımda kendi kelime kurallarını korur (daha isabetli), hâl/çekim/kelime sırasında kural metnini + örneği verir, fiil yerinde metni korur ama bağlantıyı kuraldan alır (weil → a2-nebensatz). Drill gerekçeleri şimdilik kendi metinleriyle; kural kimliğine bağlanmaları adım 3'te. Claude.
- 2026-08-26 — Soru türleri tek `SkillQuestion` şemasında kaldı (`kind` + `accept[]`/`items[]`; `options: []`, `answer: 0`): ayrı tip birleşimi 344 egzersizin doğrulayıcısını ve DB `data` jsonb'sini kırardı. Yazılı cevap kabulü umlaut/büyük-küçük/noktalama bağımsız, ≥5 harfte tek harflik sapma da geçer (anlama ölçülüyor, yazım değil). `truefalse_why` ayrı tür değil: mevcut `truefalse` + `explain` zaten gerekçeli. `reply` ve `summary` serbest yazma oynatıcısına adaptörle giriyor (`asFree`): üçü de "yaz, rubrik puanlasın". `form` tam (toleranslı) eşleşme, ≥%70 alan doğruysa tamam; `rewrite` WP-10 `matchSentence` + gerekçe (drill ile aynı dil). Dinlemede gerçek ses dosyası WP-72 kaydı gelince (`segments[].audio` alanı var, dikte TTS ile). Her okuma/dinleme egzersizine ≥2 yazılı soru: doğrulayıcı uyarısı baseline'a alındı (238), içerik WP-72'de. Claude.
- 2026-08-26 — Rol yapma sınavı ayrı bileşen (`roleplay-exam.tsx`), ders oynatıcısına mod eklenmedi: oynatıcı 1800 satır ve üç fazlı, sınav tek fazlı ve yardım makinesi olmayan bir ekran — ortak nokta yalnız `/api/roleplay` ve sahne. Sınav istemi alıştırma isteminin düzeltme/öneri bloklarını TAŞIMAZ ("düzeltme yazma" demek yerine yokluğu): model gördüğü hatayı sessiz geçiyor, kanıt: "Am Abend ich koche" konuşmada düzeltilmedi, rubrik yakaladı (fiilin yeri). Geçme eşiği rubrik toplamı ≥60 (yazma kartıyla aynı). Mikrofon tek atış, tanıyıcı yoksa yazı; ikisi sınavda eşdeğer (telaffuz WP-20). Servis yoksa sınav senaryolu konuşmaya DÜŞMEZ — ölçüm sayılmaz, ekran bunu söyler. Seviye sınavının (WP-41) konuşma bölümüne bağlama, WP-20 ile birlikte. Claude.
- 2026-08-26 — Monolog üçüncü konuşma biçimi (`monologue` alanı; `dialogue`/`tasks` ayrımı korunuyor), `itemCount`=1. Kayıt iki katman: MediaRecorder (kendi kaydını dinle, her cihaz) + tanıyıcı (transkript). Transkript puanlanmadan önce DÜZENLENEBİLİR: tanıyıcı aksanlı Almancada kelime kaçırıyor; bedel, düzeltmenin dilbilgisi puanını değiştirebilmesi — alıştırma, sınav değil (sınav için WP-22). Tanıyıcısız/sağlayıcısız yol öz değerlendirme: tamamlanma sayılır, puan kaydedilmez, kart bunu söyler. Sözlükçe boş: kalıplar `targets`te, oynatıcı gösterir (doğrulayıcının `en` borcu büyümesin). Drill temaları (40) içerik işi: WP-72 ile, `speech-rules` türetimi mevcut. Claude.
- 2026-08-26 — Açık diyalogda kalıp işaretlemesi modele bırakılmadı, yerel gövde eşleştirmesi (`targetsUsed`, "/" seçeneklerinden biri yeter): küçük modeller işaret satırlarını gövdeye karıştırıyor — kanıt, öneri satırlarının cevaba yapıştığı ilk çalıştırma; öneri isteği istemden çıkarıldı. Düzeltme yok (anlama/akış alıştırması; düzeltme dersin rol yapmasında). Payda açık modda 4 (tamamlanma eşiği), senaryoda tur sayısı. Servis yoksa GET `/api/roleplay` senaryoda tutar; akış koparsa senaryo baştan devralır ve bunu söyler. Züritüütsch diyalogları da temalı (lehçe notu istemde). Claude.

## Döngü kapanışı — 2026-08-26 (Claude)

Otonom yapılabilen bütün WP'ler işlendi: Faz 0 (WP-00..04), üretim alıştırmaları (WP-10..14), yazma (WP-12/30/31), konuşma (WP-21/22/23), ölçme (WP-40..43), analitik (WP-50..52), arayüz (WP-60..66), içerik hattı (WP-70, WP-73 adım 1–2). Satırlar `inceleme`/`kısmen`; `bitti` işareti sahibinin.

**Sahibin kararına/eline bağlı kalanlar**
- WP-20 Telaffuz puanlama: Azure Speech mi Deepgram mı — 20 kayıtlık karşılaştırma (`docs/plan/pronunciation-eval.md` boş). Karar gelince WP-22 sonucu ve WP-41 konuşma bölümü bağlanır.
- WP-71/72/73 içerik ölçekleme: B1–C1 ders kapsamı, her okuma/dinlemeye ≥2 yazılı soru (238 borç, baseline'da), 40 telaffuz drill'i, 18 diyalog, 55 tablo drill'i, karıştırma çiftleri — LLM aday üretimi + gözden geçirme; içerik doğrulayıcılar hazır (`test:content`, `test:cheat`, `test:rules`).
- Kullanıcı testleri: WP-40 kalibrasyon (10 kişi), WP-62/65 (3 kişi), WP-66 koç cümlelerinin tonu.
- Üretim: migrasyonlar 0026–0034 uygulandı; `db:seed:skills` ile 364 egzersiz yüklü; Vercel cron'lar `vercel.json`'da (assess 6 saatte bir, özet Pazartesi 07:00).
- 2026-08-26 — WP-20 sağlayıcı: Azure sahibin kararıyla dışarıda. Almanca için ücretsiz fonem API'si yok (Speechace Almanca yok, SpeechSuper 20 $/ay taban). Karar: Groq Whisper large-v3-turbo (günde 28 800 sn ses, anahtar var) + kelime hizalamalı kendi puanlama; yedek Gladia (10 sa/ay), Speechmatics (8 sa/ay), Deepgram (200 $ kredi). Fonem düzeyi faz 2: HF Space'te wav2vec2-xlsr-53-espeak (ücretsiz CPU). Tarama ve tablo `pronunciation-providers.md`. Claude.
- 2026-08-26 — İçerik ölçekleme yöntemi: türetilebilen türetildi (okuma/dinleme yazılı soruları metnin sözlükçe kelimesi ve mevcut şıkların doğru cevabından; `content:derive` deterministik, elle yazılan sorular öncelikli), yazılması gerekenler elle yazıldı (drill 276, telaffuz drill 40, diyalog 18, yazma görevi 40, karıştırma çifti 150, B1 ders 10). Uyarı bütçesi iki kez bilinçli genişletildi (`en` karşılığı olmayan sözlükçe girdileri) — içerik borcu olarak duruyor. Türetilmiş sorular gözden geçirme için `derived-questions.ts`'te okunabilir; yanlış bulunan egzersize elle soru yazılınca türetme o egzersizden düşer. Claude.
- 2026-08-26 — WP-20 faz 1 uygulandı: `/api/stt` ve `/api/pronounce` aynı zincir (`lib/stt.ts`); sıra kota ölçümüne göre Groq → Cloudflare (REST, Worker yok) → Speechmatics → Deepgram → Mistral; 429/400/5xx'te sonraki sağlayıcı. Puan kelime hizalaması (WP-10 `matchSentence`) + bütünlük + akıcılık (Groq kelime zaman damgası; hece hızı 2–4,5/sn doğal, 0,5 sn üstü duraklama −10); eşik 80. Sınır dürüstçe: Whisper yakın sesteşleri düzeltiyor (TTS ile "Staat" söylenen klip "Stadt" döndü) — fonem hatası ancak faz 2 (wav2vec2) ile; kart "kelime düzeyi" der. Söyleyiş turunda kayıt tanıyıcıyla paralel: karar anında, puan 0,3–1 sn sonra. pocket-mic dilimleri artık 16 kHz WAV (bozuk webm → 400 sorunu). Günlük kullanıcı kotası 120 istek (`ai_usage` sayacı). Claude.

## 26 Ağustos akşamı — açık kalanların kapatılması (/goal)

Karşılaştırma raporundaki (reports/gelisim-karsilastirma.html) açık kalanlar üçe ayrıldı:

**Kapatıldı.**
- WP-80 takip katmanı: 50 olay, `test:events`, `report:events`/`report:learning` yeni bölümler, `report:all`, `docs/plan/80-takip.md`. "Veri yok" durumuna düşmemenin kuralları orada.
- Sınava konuşma bölümü (WP-41): kelime düzeyi telaffuz puanı, STT yoksa bölüm yok; kâğıt demo sunucuda doğrulandı (seviye 3, modül 2 madde).
- Üretim oranı (WP-14 / KPI 2): iki neden bulundu — tanım cevap sayıyordu (düzeltildi: tur sayar) ve fresh/shaky kelimede merdiven üretim vermiyordu (üretim tabanı `PRODUCTION_FLOOR`=0.4; simülasyon %28 → %45, demo hesabında yeni oturum 7/16).
- Okuma/dinleme gerekçesi: zaten vardı (1.547 soruda `explain`); rapor düzeltildi.
- Ölçüm kartlarının görünürlüğü ve STT kotası: artık ölçülüyor (`panel_open`, §14 sağlayıcı başarı/429).

**Bilerek açık (ağır içerik üretimi, sahibin ertelemesi).** Ders kapsamı kapandı (A1–C1 500 ders); B1–C1 tablo drill'leri (32 tablo); gerçek dinleme kayıtları; fonem düzeyi telaffuz (HF Space, faz 2).

**Sahibe bağlı.** Yerleştirme kalibrasyonu, telefonda mikrofon testleri (telaffuz kartı, sınav konuşma bölümü), koç tonu, WP-62/65 akış testleri; `report:all` ile haftalık okuma.

- 2026-08-26 — C1 (100 ders + 10 modül sınavı) üretildi ve müfredat A1'den C1'e KAPANDI: 500 ders, 50 sınav. C1'in B2'den farkı yeni kural değil SEÇİM — aynı içeriği kaç ayrı üslupta söyleyebildiği ve hangisini neden seçtiği ölçülüyor. Bu yüzden modüller tek bir yapıyı değil bir register'ı çalıştırıyor: kurumsal işlev fiili öbekleri, kip parçacıklarının tonu, retorik mimari, deyimlerin anlamı, tam dolaylı aktarımın sorumluluğu, hukuk dilinin bedeli, katılım öbeklerini açmak, imtiyazla tartışmak, ironiyi duymak. Doğru/yanlış maddelerinin bir kısmı bilerek dilbilgisini değil ANLAMI sınıyor (olumsuz soruya "nein", "Nicht schlecht"in övgü olması, aktarım kipinin kanıt olmaması, scheinbar/anscheinend farkı) — çünkü C1'de kusursuz cümleyle yanlış şey söylemek mümkün. `FOCUS_SHEETS`e C1'in on dört odağı eklendi; C1 tabloları uzun kalıp taşıdığı için sınavın dilbilgisi bölümüne az hücre veriyor, alt seviye sayfaları boşluğu kapatıyor. Rol yapma tur sayısı C1'de de 8-9. Claude.
- 2026-08-26 — B2 (100 ders + 10 modül sınavı) üretildi. Seviye farkı kelime listesinde değil dilin İŞİNDE: B1'de öğrenci kendi hayatını anlatıyordu, B2'de bir kurumun dilini konuşuyor — fiili isme çeviriyor (nach der Prüfung), eylemi failinden ayırıyor (edilgen ve yerine geçen üç yapı) ve bir cümlenin kim tarafından, hangi bağlayıcılıkla söylendiğini dilden okuyor (aktarım kipi; soll/will/dürfte üçü de Türkçeye "-mış" diye çevrilir ama üçü ayrı kaynak gösterir). Doğru/yanlış maddeleri modül içinde hata tipine göre dağıtıldı — bir modülde aynı hata iki kez sorulmuyor; toplam 100 madde, 30 doğru / 70 yanlış. Rol yapma tur sayısı B2'de de 8–9: üst sınır dokuz olduğu için seviyeyle artış duruyor, B2'de yük tur sayısında değil cümlenin kendisinde. `FOCUS_SHEETS`e B2'nin 14 odağı eklendi ve her odak kendi B2 sayfasının yanında dayandığı B1 sayfasını da açıyor — keyfî değil, sınavın dilbilgisi bölümü için gereken hücre sayısını garanti ediyor. Claude.
- 2026-08-26 — Modül sınavı v3: kâğıdın her bölümü modülün KENDİ içeriğinden. Karar kaydı: (1) dersten türetilebilen türetildi — üretim adımları Satzbau bölümüne, doğru/yanlış adımları dilbilgisine, ders kelimeleri Wortschatz'a, ders odakları (`focusId`) tablo sayfalarına köprüyle (`FOCUS_SHEETS`); (2) türetilemeyen elle yazıldı — 23 modül için dinleme diyaloğu, okuma metni, konuşma cümleleri, yazma görevi ve yapabilirlik listesi (de/tr/en). Metinler modülün görmediği yapıyı taşımıyor (A1.5 metninde Perfekt yok, A1.6'da du-emri yok). (3) Puan madde sayısına göre değil BÖLÜM AĞIRLIĞINA göre: üretim bölümleri (Satzbau 25 + Sprechen 15 + Schreiben 10) toplamın yarısı; eskiden yazma bölümü kâğıdın %4'üydü. (4) Sınav havası kâğıdın kendisinden: kapak (ne ölçülüyor, kural), bölüm arası (Teil n/N), ipucu yok (oyunlarda `NoHints` bağlamı), cevap sınav bitmeden gösterilmiyor; bitince yapabilirlik listesi ve kaçırılan maddelerin dökümü. (5) Yol haritasının tacı hız turundan sınava taşındı; hız turu ısınma olarak kaldı ve bunu söylüyor. Bulunan iki kusur düzeltildi: tablo hücrelerinde tekrar eden çeldiriciler (aynı şık iki kez) ve elle yazılan soruların şık sırası (artık tohumlu karıştırılıyor). Doğrulama veritabanısız: `test:exams` + `test:exam-build`. Claude.

- 2026-08-26 — B1 tamamlandı (modül 4–10, 70 ders) ve her modüle sınav planı yazıldı. Karar kaydı: (1) Pilot partinin (modül 3) kalıbı korundu ama bir hatası düzeltildi — beş kelimenin yalnızca dördü söyletiliyordu, `check:lessons` bunu on iki hata olarak sayıyordu; eksik tekrar adımları eklendi ve üç dersin adım sayısı alt sınıra çıktı. (2) Modül teması bir dilbilgisi kuralına değil bir DİL İŞLEVİNE bağlandı: tanımlama (4), resmî işlem (5), hedef (6), görüş (7), sağlık sistemi (8), toplumsal düzen (9), gerçek olmayan (10). Aynı yapı birden çok modülde dönüyor; her modülde tek yeni kural var. (3) Her derste 5 kelime + 4 üretim + 1 doğru/yanlış; doğru/yanlış cevaplarının %30'u doğru (katalog hedefi %25–60). Elli kelimenin hepsi seviye içinde yeni. (4) Doğru/yanlış adımları rastgele değil, Türkçe konuşanın en inatçı hatasına ayrıldı: karşılaştırmada "wie/als", edilgen Perfekt'te "worden", geçmiş dilek kipinde mastar/ortaç, kendiliğinden değişimde edilgen kurmama. (5) Sınav planları da modülün kendi sahnesinde: kayıp eşya bürosu, yabancılar dairesi, kurs danışmanlığı, münazara, aile hekimi, komşu toplantısı, ara bilanço. `test:exams` iki gerçek kopukluk yakaladı (Infinitiv-zu ve Konjunktiv-II-irreal odakları tablo haritasında yoktu) — ikisi de eklendi. Claude.
- 2026-08-27 — Yürürken modu ses tanıma: (1) ekran açıkken yalnız tarayıcı tanıyıcısı — iki boş dinlemede oturum boyu vazgeçme kaldırıldı, sessizlik 4→7 sn, hata kodu kayda (`walk_listen`); (2) ekran kapalı yol için Azure Speech F0 (`germanywestcentral`; "Azure yok" kararı hesap açılamamasından kaynaklanıyormuş, çözüldü) zincirin başına, yalnız `mode: walk`ta ve görünürlükten seçilerek — görünür sayfa Azure isteyemez; aylık 4,5 sa tavan `ai_usage`'dan; (3) klip PCM'e çözülüp konuşma bölgesine kırpılıyor (`lib/vad`): ölçümde giden ses %80 azaldı, güven düşmedi, sessiz pencere gönderilmiyor; (4) geçişler iki yönde yakalanıyor (dinlemede kapanma → soru tekrar + cep yolu; açılınca süren kayıt bekleniyor), süresi dolan dinleme iptal ediliyor. Azure telaffuz puanı ölçüldü: karışan çiftleri ayırıyor ama temiz seste kelime puanı 44–100 dalgalı, fonem sembolleri boş — WP-20'ye bağlanmadan kalibrasyon şart (`walk-stt.md`). `test:walk` `visible-only`/`switch`, `test:vad`. Claude.
- 2026-08-27 (akşam) — Yürürken v2: ilk düzeltme sahada düştü — `walk_listen` altı dinlemede altı `browser:end`; oturum başında tutulan mikrofon akışı (parçaları kapalı) Android'de tanıyıcıyı sağır bırakıyor, Bluetooth'ta okumayı SCO'ya düşürüyor. Ekranda kip artık dersle birebir aynı (mikrofon tutulmaz, sessiz döngü yok, okuma oyunların boşluksuz yolundan, işaret dersin işareti); cep yolu "Cebe koy" ile dokunuşun içinde kuruluyor (mikrofon kilitli ekranda istenemiyor), ekran kapanınca kayıt, açılınca kendiliğinden ekran kipi; ekran kipinde ekran kapanırsa tur durup çaresini söylüyor. Düğmeden doğrudan okumak döngüyü 30 sn asıyordu, duyuru döngüye bırakıldı. Harness: sahte tanıyıcı ekrandaki Türkçeden cevap veriyor, `switch`te `stop()` sonrası `onend`. Bkz. `walk-stt.md` v2. Claude.
- 2026-08-27 (v3) — Cep yolu sahada ölü çıktı: "Cebe koy → ekranı kapat → hiçbirini duymadı". `ai_usage` kök nedeni verdi — klip WAV'a çevrilemiyor, ham webm gidiyor, Azure "desteklenmeyen biçim webm", ötekiler "bozuk dosya". Sebep: `new AudioContext()` kilitli ekranda suspended başlıyor, `decodeAudioData` çözmüyor. `decodePcm` OfflineAudioContext'e taşındı (donanımsız, render güdümlü). Gözlem: `stt:silent`/`stt:decode` ayrıldı, silent'ta tepe-dB kaydı; TTS okuma yolu kipe değil görünürlüğe bağlandı (cepte ekran açıkken de boşluksuz WebAudio). Uyarı: OfflineAudioContext'in kilitli ekranda çözdüğü doğrulanmadı; kalırsa Plan B oneShotClip→Deepgram. Claude.
- 2026-08-27 (v4) — Client-side ses çözme kilitli ekranda imkânsız (v3 OfflineAudioContext de düştü: `stt:decode`, `ai_usage`'da karşılık yok). O yol bırakıldı: cep yolu her cevap için taze `MediaRecorder` ile GEÇERLİ webm üretiyor (`recordFreshClip`), sunucu ham çözüyor, decode/VAD yok. `walk` zinciri Deepgram önde (webm native, başı-kesikte uydurmuyor); Azure webm almadığı için STT'den çıktı (yalnız TTS yedeği, `azure()` adaptörü ileride WAV için duruyor). Ekran açık turlar arası ~0,55 sn nefes. Ölü kod (halka tampon: recordClip/activateMic/oneShotClip, lib/vad cep tarafı) ayrı temizliğe bırakıldı. Veri müdahalesi: Samet'in (7427d7…) yürürken resume'u (idx=19 yarım tur, loop'ta) `session_state`'ten silindi; user_words/reviews korundu. Claude.
