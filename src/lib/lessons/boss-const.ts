/**
 * Hız turunun istemcinin de bilmesi gereken sayısı.
 *
 * `lib/lessons/boss` `server-only`; sınav ekranındaki bağlantı etiketi
 * ("oyun: hız turu · modülün kelimeleri, {n} sn") ise iki platformda
 * sözlükten geliyor. Süre sözlüğün içinde düz metin durduğu sürece süre
 * değişince etiket eski sayıyı söylemeye devam ederdi.
 */

/** Başlangıç süresi (saniye). */
export const BOSS_SECONDS = 60;
