/**
 * Onboarding'in görülüp görülmediği — AsyncStorage anahtarı tek yerde.
 * "1" yazılıysa ilk açılış akışı atlanır (bkz. OnboardingScreen, RootStack).
 */
export const ONBOARDED_KEY = "nomi:onboarded";
